version: '3.8'
services:
  router:
    build: .
    volumes:
      - .:/app
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    stdin_open: true # docker run -i
    tty: true # docker run -t
